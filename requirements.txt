# Runtime dependencies for the Sable stock-alert system.
# Install: pip3 install --no-cache-dir -r requirements.txt
# Dev/test tooling lives in requirements-dev.txt.
#
# Floors pinned to the versions the live host runs on (2026-06). Regenerate
# after any deliberate upgrade so a fresh `podman build` matches the host.

# --- Core data / numerics (used by ~23 modules) ---
pandas>=3.0.1
numpy>=2.4.3
scipy>=1.17.1

# --- Market data ---
yfinance>=1.2.0
kiteconnect>=5.0.0
growwapi>=1.5.0
pyotp>=2.9.0
nselib>=2.5.1
requests>=2.32.5

# --- Forecasting / regime modeling ---
statsmodels>=0.14.6
prophet>=1.3.0
hmmlearn>=0.3.3

# --- Alert daemon: config, env, scheduling, calendar ---
python-dotenv>=1.2.2
pytz>=2026.1
radicale>=3.6.1
icalendar>=7.0.3

# --- Web UI / API (web/) ---
fastapi>=0.135.3
uvicorn>=0.44.0
pydantic>=2.12.5

# --- News + Discord ingest ---
beautifulsoup4>=4.14.3
feedparser>=6.0.12
discord.py>=2.7.1

# --- TUI + terminal charts ---
textual>=8.1.1
rich>=14.3.3
plotext>=5.3.2

# --- PDF report rendering ---
weasyprint>=68.1

# --- NSE scraper fallback (deprecating; FII/DII now via nselib — kept one release) ---
playwright>=1.60.0
playwright-stealth>=2.0.3
